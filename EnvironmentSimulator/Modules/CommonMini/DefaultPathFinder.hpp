#include <string>

namespace esmini::common
{
    class DefaultPathFinder
    {
        public:
            std::string GetDefaultPath();

        private:
            //std::string GetLibraryPath();
    };
}