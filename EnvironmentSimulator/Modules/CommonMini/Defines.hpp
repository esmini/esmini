// File to store the defines and common types used in the project

#include <string>

const std::string CONFIG_FILE_OPTION_NAME = "config_file_path";

const std::string DEFAULT_CONFIG_FILE = "config.yml";
