// {{name}}.hpp
// Autogenerated by Jinja
{% import 'macros.j2' as m %}


#include <string>
#include <vector>
{%if name != "Core"-%}
#include "Core.hpp"
    {%if name != "Junction"-%}
#include "Junction.hpp"
    {%endif-%}
    {%if name != "Lane"-%}
#include "Lane.hpp"
    {%endif-%}
    {%if name != "Road"-%}
#include "Road.hpp"
    {%endif%}
    {%if name != "Object"-%}
#include "Object.hpp"
    {%endif%}
    {%if name != "Signal"-%}
#include "Signal.hpp"
    {%endif%}
    {%if name != "Railroad"-%}
#include "Railroad.hpp"
    {%endif%}
{%endif-%}


namespace {{name}}{
{{m.generate_datastructs(data)}}
}

